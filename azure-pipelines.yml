# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: Default

steps:
- task: CmdLine@2
  inputs:
    script: 'oc login https://172.30.0.1:443 -u kubeadmin -p $(OCP_PASSWORD) --insecure-skip-tls-verify=true'
  displayName: Login to OpenShift
- task: CmdLine@2
  inputs:
    script: 'oc project myapp'
  displayName: Switch to project
- task: CmdLine@2
  inputs:
    script: 'oc start-build sample-spring-boot-web --follow'
  displayName: Start build
- task: CmdLine@2
  inputs:
    script: 'oc status'
  displayName: Check status





